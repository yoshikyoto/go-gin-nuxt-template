package route

import (
	"github.com/gin-gonic/gin"
)

func SetRoutes(engine *gin.Engine) *gin.Engine {
	//api := engine.Group("/api")
	//api.get("/", controller.NewController().Index)
	return engine
}
